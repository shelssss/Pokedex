<!DOCTYPE html>
<html lang="en">
<head>
  <%- include("../partials/header") %>
</head>
<body class="bg-red-200 text-gray-800">
  <div class="container mx-auto p-5">
    <h1 class="text-white text-3xl font-bold text-center mb-5">Pokédex</h1>

    <!-- 🔍 Search Form -->
    <form action="/search" method="GET" class="flex justify-center mb-5">
      <input type="text" name="name" placeholder="Search Pokémon..." class="px-4 py-2 border rounded-l-lg focus:outline-none">
      <button type="submit" class="bg-red-400 text-white px-4 py-2 rounded-r-lg">Search</button>
    </form>

    <!-- Pokémon List -->
    <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
      <% pokemonList.forEach(pokemon => { %>
        <a href="/pokemon/<%= pokemon.name %>" class="p-6 bg-red-400 shadow rounded-lg text-center hover:shadow-xl">
          <p class="text-white text-xl capitalize font-semibold"><%= pokemon.name %></p>
        </a>
      <% }); %>
    </div>
  </div>
  <%- include("../partials/footer") %>
</body>
</html>